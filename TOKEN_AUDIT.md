# Аудит расхода токенов

Что измерялось, что выяснилось и что с этим сделано.

Все цифры получены прогоном `./gradlew benchmark --args="baseline"` — 10 задач через настоящего агента на `qwen3:14b` с настоящим retrieval. Сырые данные лежат в `reports/`, туда же пишется экспорт после каждого прогона.

## Как измеряется

Middleware стоит вокруг каждого вызова модели (`MeteredLlm`) и каждого вызова инструмента (`MeteredTool`); единица измерения — run, от сообщения пользователя до ответа.

Что откуда берётся — важно, потому что не всё из этого сообщает провайдер:

| Метрика | Источник |
| --- | --- |
| `input_tokens`, `output_tokens` | от провайдера (Ollama через Koog их отдаёт) |
| `latency`, `turns`, `tool calls` | измеряется напрямую |
| `estimated_cost` | токены × прайс `COST_REFERENCE_MODEL` |
| `reasoning_tokens` | наша оценка: `<think>`, который вырезается перед показом; ограничен сверху измеренным output |
| `reused_input_tokens` | наш подсчёт: дословный префикс, совпадающий с промптом предыдущего turn'а |
| разбивка контекста | сумма измерена, пропорции оценены по сообщениям и отмасштабированы к ней |

`cached_tokens` всегда 0 и это не пропуск: Ollama кеш не сообщает, а провайдерского кеша здесь нет. Вместо выдуманного «cache hit» считается `reused_input_tokens` — то, что кеш обслужил бы.

## Базовая линия

```text
BASELINE
────────────────────────────────────────

Tasks completed                       10

Total tokens
  Input                          16.8k
  Output                          3.6k
  Reused input                     912
  Reasoning (discarded)           3.4k

Estimated cost                   $0.0047
  billed as                gpt-4o-mini

Average task
  Tokens                          2.0k
  Turns                            1.9
  Tool calls                       0.9
  Latency                        15.3s

Repeated input share                  5%
Success rate                        100%

Most expensive tools:
  search_documents  100%  (9.1k, 8 calls)
  current_datetime   0%  (12, 1 calls)

Context growth:
  Tool outputs           ███████████          53%
  Tool schemas           ██████               29%
  Conversation history   ██                   9%
  System prompt          ██                   8%
  User task                                   1%
  Chat template etc.                          0%
  (the total is measured; the parts are estimated, and what could not be
   attributed is its own line rather than spread across the others)
```

По задачам — `reports/baseline-runs.csv`; по каждому вызову модели и инструмента — `reports/baseline-calls.csv`.

## Что нашлось

**1. Выдача `search_documents` — 53% всего входного контекста.**
Один вызов отдаёт около 4 200 символов ≈ 1 120 токенов: 5 чанков по 1000 символов целиком плюс служебная обвязка. Это 100% токенов, порождённых инструментами: `current_datetime` на их фоне — 12 токенов.

**2. Размышления съедают почти весь выход.**
Reasoning ≈ 95-100% от output-токенов. За них платят полностью, а пользователь их не видит: `<think>` вырезается и не попадает даже в историю.

**3. Схемы инструментов — 29% входа**, и они уходят в каждый запрос целиком.

Это выяснилось не сразу: первая версия разбивки приписывала эти токены системному промпту и показывала «21%». Замер вычитанием на живой модели:

```text
только вопрос пользователя     24 токена
+ системный промпт             97   →  системный промпт =  73
+ схемы инструментов          343   →  схемы            = 246
```

Настоящий системный промпт — 8% входа, то есть сжимать его почти бессмысленно. Описания инструментов весят втрое больше и отправляются столько же раз.

**4. Самый дорогой turn — второй.** В среднем ~1 370 входных токенов против ~460 на первом. Разница — ровно та выдача retrieval, которая приезжает после первого вызова инструмента.

**5. Повторного контекста почти нет — 5%.**
Это расходится с примером в задании, где повторов 73%. Причина честная, а не хорошая: диалоги в наборе короткие (1.9 turn'а), повторяться попросту нечему. На длинных диалогах эта доля вырастет, но оптимизировать её здесь означало бы экономить на пятипроцентной статье, пока рядом лежит шестидесятипятипроцентная.

## Что сделано

| Оптимизация | Целится в | Итог |
| --- | --- | --- |
| Выдача `search_documents`: top-3 вместо 5, окно ~400 символов вокруг релевантного места, без служебной обвязки | находка 1 | выдача инструмента упала с 9.0k до 2.6k токенов |
| Отключено thinking (`OllamaParams(think = false)`) | находка 2 | выход упал на 86%, задержка — на 82% |
| Сжаты описания инструментов | находка 3 | схемы уменьшились, поведение сохранено |
| ~~Сжать системный промпт~~ | находка 3 | **откачено**, см. ниже |
| Компактная история | находка 5 | не потребовалось: порог взят и без неё |

## Результат

```text
BEFORE / AFTER
────────────────────────────────────────────────────────
                           baseline  optimized    change

Tokens per task                2.0k        936    -54.2%
  input                        1.7k        885    -47.4%
  output                        360         51    -85.8%
  repeated input                 91         91      0.0%
Cost per task, USD           0.0005     0.0002    -65.1%
Turns per task                  1.9        1.9      0.0%
Tool calls per task             0.9        0.9      0.0%
Latency per task, s            15.3        2.8    -82.0%

Success rate                   100%       100%      0.0 pp

Target: tokens −30% or better, success rate no worse than −2pp
MET: tokens -54.2%, success rate +0.0 pp
```

Дашборд после оптимизаций:

```text
OPTIMIZED
────────────────────────────────────────

Tasks completed                       10

Total tokens
  Input                           8.9k
  Output                           509
  Reused input                     912
  Reasoning (discarded)              0

Estimated cost                   $0.0016
  billed as                gpt-4o-mini

Average task
  Tokens                           936
  Turns                            1.9
  Tool calls                       0.9
  Latency                         2.8s

Repeated input share                 10%
Success rate                        100%

Most expensive tools:
  search_documents  100%  (2.6k, 8 calls)
  current_datetime   0%  (12, 1 calls)

Context growth:
  Tool schemas           █████████            44%
  Tool outputs           ███████              33%
  System prompt          ███                  17%
  Conversation history   █                    3%
  User task              █                    3%
  Chat template etc.                          0%
  (the total is measured; the parts are estimated, and what could not be
   attributed is its own line rather than spread across the others)
```

Побочный эффект, которого не искали: **задержка упала с 15.3 до 2.8 секунд** — почти вся она уходила на генерацию размышлений.

Состав контекста изменился. Теперь самая крупная статья — **схемы инструментов (44%)**, потому что всё остальное ужалось вокруг них. Это и есть следующая цель, если понадобится ещё экономия.

## Отрицательный результат: сжатие системного промпта

Системный промпт сократили с 366 до 208 символов. Экономия — 8% входа, цена — **10 pp success rate**, и оптимизация откачена.

Сломалась задача `siri-languages`: вопрос «Which language does the new Siri launch in first?» не содержит слов «в моих документах», и с укороченным промптом агент перестал вызывать retrieval — отвечал по памяти модели. В одном из прогонов он при этом **выдумал имя файла** `siri_launch_info.txt`, которого не существует.

Причину подтвердили изоляцией: с прежним промптом и всеми остальными оптимизациями — 100% success. Промпт возвращён, формулировка помечена в коде как несущая.

Похожим образом сначала пострадало и описание `search_documents`, урезанное до «Searches the user's uploaded documents». Оно возвращено к промежуточному варианту, в котором осталась фраза о том, *когда* инструмент нужен.

## Чего стоила чистота сравнения

Дважды приходилось перезамерять обе ветки, потому что менялись правила измерения:

- добавили отдельный учёт схем инструментов — перемерили baseline;
- переписали предикат для задач без ответа — перемерили обе ветки.

Второй случай стоит пояснить. Задачи `absent-*` проверялись списком формулировок отказа, и список дважды не совпадал с корректным ответом агента («do not mention», «None of the chunks mention») — выглядело это как падение качества, хотя агент вёл себя правильно. Предикат переписан на то, что задача требует на самом деле: агент обязан выполнить поиск и не имеет права назвать цифру. Список формулировок измерял словарь модели, а не её честность.

## Что измерение поймало в себе самом

Аудит несколько раз ловил ошибки в собственных цифрах — это стоит отдельного упоминания, потому что неверная метрика отправила бы оптимизацию не туда:

- **Повторный контекст считался как 93%** вместо 5%: `AgentLoop` передаёт модели тот же мутабельный список, в который потом дописывает, а метрика хранила ссылку на него вместо снимка. Второй turn выглядел дословной копией первого.
- **`reasoning_tokens` были нулём**: Koog кладёт размышления в отдельный `MessagePart.Reasoning`, а считался только вырезанный из текста `<think>`.
- **Остаток от масштабирования разбивки** всегда падал в «историю», из-за чего на первом turn'е, где истории нет, стоял ненулевой размер.
- **`task_id` не записывался** — сравнение «до/после» было бы возможно только по среднему, а среднее скрывает сломавшуюся задачу.
- **Недоступная модель давала «Success rate: 0%»** вместо ошибки: 10 запусков по нулю токенов записывались как полноценное измерение.
- **Схемы инструментов не считались вовсе**, а разница между оценкой и реальным `input_tokens` размазывалась по категориям пропорционально. Из-за этого системный промпт выглядел как 21% контекста вместо 8%, а вторая по величине статья расхода — 29% на схемы — не была видна совсем.

На каждый случай есть регрессионный тест.
